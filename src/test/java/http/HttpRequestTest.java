package http;

import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.List;

import static org.junit.jupiter.api.Assertions.assertDoesNotThrow;

class HttpRequestTest {

    @Test
    void create_http_request() {
//        List<String> request = Arrays.asList("GET /index.html HTTP/1.1",
//            "Host: localhost:8080",
//            "Connection: keep-alive",
//            "Accept: */*");
//
//        String requestLine = request.get(0);
//        List<String> header = request.subList(1, 4);
//
//
//        assertDoesNotThrow(() -> new HttpRequest(new RequestLine(), header));

    }
}